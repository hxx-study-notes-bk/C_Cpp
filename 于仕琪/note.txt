用makefile会面临一个问题，就是makefile配置时，跟你当前使用的系统强相关，比如说你在linux下面写的makefile，放到windows下面，那就会有问题，因为它编译器会不同，路径会不同，各种各样的东西都会不同，如果你要开发一个跨平台的这么一个软件，难道你还要给不同的操作系统不同的编译环境里面写不同的makefile，这个事情非常繁琐，怎么解决呢，用cmake，CMake可以生成不同平台下的Makefile，有了CMake不用再写复杂的Makefile